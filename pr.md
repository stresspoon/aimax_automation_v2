## 요약
저장소 정리 및 SNS/엑셀/자동선정 로직 개선. 관련 유닛 테스트 추가.

## 주요 변경

### 1. 저장소 정리
- **dev 스크립트 이동**: `scripts/dev/` 디렉토리로 정리
- **SQL 보조 스크립트 이동**: `supabase/scripts/` 디렉토리로 정리
- **trash/playground 제거**: 불필요한 파일 및 디렉토리 삭제
- **Yarn 기준 통일**: `package-lock.json` 제거
- **`.gitignore` 개선**: `*.bak`, `*.tmp` 패턴 추가
- **`tsconfig.json` 정리**: exclude 경로 추가

### 2. 기능 개선

#### SNS 재체크
- 전체 프로젝트 데이터로 저장하여 재방문 시 수치 유지
- 체크 상태 변경이 올바르게 반영되도록 개선

#### 후보 리스트
- 자동 선정 기준으로 상태 재계산
- merge 로직 개선

#### 엑셀 연동
- 값이 있는 우선순위 컬럼만 포함
- 원본 폼 데이터는 `data` 필드로 포함하여 커스텀 필드 반영

### 3. 코드 품질 개선
- **빈 catch 블록 제거**: 적절한 에러 로깅 추가
- **환경 변수 타입 안전성**: `src/lib/env.ts` 추가
- **HTTP 표준화**: `fetchJSON` 사용으로 변환
- **토스트 중복 방지**: `src/lib/toast.ts` 헬퍼 추가

### 4. 테스트 추가
- `tests/sns.normalizeUrl.test.ts`: URL 정규화 테스트
- `tests/measure.route.test.ts`: 측정 라우트 테스트
- `tests/candidates.merge.test.ts`: 후보 병합 로직 테스트
- `tests/projects.normalize.test.ts`: 프로젝트 정규화 테스트

## 변경 사항 요약
- **추가된 파일**: 23개 (테스트 파일, 유틸리티, 이동된 스크립트)
- **수정된 파일**: 8개 (리팩토링 및 기능 개선)
- **삭제된 파일**: 51개 (trash 디렉토리, 백업 파일, 불필요한 이미지)

## 테스트
- 모든 테스트 통과 확인
- SNS 재체크 기능 정상 동작 확인
- 엑셀 연동 기능 정상 동작 확인

## 체크리스트
- [x] 코드 리뷰 요청
- [x] 테스트 작성 및 통과
- [x] 로컬 환경에서 정상 동작 확인
- [ ] 배포 후 프로덕션 확인 필요